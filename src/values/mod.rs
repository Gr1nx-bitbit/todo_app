pub const BASE_PRIORITY_POINTS: u32 = 1;
pub const BASE_ADDITIVE_POINTS: u32 = 1; // How many points are added to a task's priority count
pub const BASE_SCALAR: u32 = 2; // Base amount a task's priority count can be multiplied by
pub const MAX_PRIORITY_COUNT: u32 = 100;